@{
    ViewBag.Title = "Login";
}

<h2>Login</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">
        @TempData["SuccessMessage"]
    </div>
}

<form action="@Url.Action("Login", "Account")" method="post">
    @Html.AntiForgeryToken()

    <div class="form-group">
        <label for="username">Username:</label>
        <input type="text" class="form-control" id="username" name="username" required />
    </div>

    <div class="form-group">
        <label for="password">Password:</label>
        <input type="password" class="form-control" id="password" name="password" required />
    </div>

    <button type="submit" class="btn btn-primary">Login</button>

    @if (!string.IsNullOrEmpty(ViewBag.Message))
    {
        <p class="text-danger">@ViewBag.Message</p>
    }
</form>


<!--@{
    ViewBag.Title = "Login";
}
<style>
    /* General reset */
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
    }

    /* Page background */
    body {
        display: flex;
        align-items: center;
        justify-content: center;
        min-height: 100vh;
        background: linear-gradient(135deg, #d0e1ff, #b0c8ff, #a0bfff, #c8e0ff);
        font-family: Arial, sans-serif;
        color: #ffffff;
        text-align: center;
    }

    /* Welcome Message Styling */
    .welcome-message {
        font-size: 2.5rem;
        font-weight: bold;
        margin-bottom: 20px;
    }

    /* Centered container for login form */
    .login-container {
        background-color: #ffffff;
        color: #333;
        border-radius: 12px;
        padding: 30px;
        text-align:center;
        width: 100%;
        max-width: 400px;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.2);
    }

        /* Login form styling */
        .login-container h2 {
        
            color: #3a0ca3;
            margin-bottom: 1.5rem;
            text-align: center;
        }

    .form-group {
        text-align: center;
        margin-bottom: 15px;
        
    }

    .form-control {
        width: 700px; /* Full width within the form group */
        padding: 8px;
        border-radius: 6px;
        border: 1px solid #ddd;
        text-align: center;
        margin: 0 auto;
    }

    .btn-primary {
        background-color: #3a0ca3;
        color: #ffffff;
        border: none;
        padding: 12px;
        width: 100%;
        border-radius: 6px;
        cursor: pointer;
    }

        .btn-primary:hover {
            background-color: #5e60ce;
        }

    .text-danger {
        color: red;
    }
</style>-->

<!-- Add Welcome Message at the top -->
<!--<div class="welcome-message">Welcome to the iCARE System</div>-->

<!-- Centered Login Form Container -->
<!--<div class="login-container">
    <h2>Login</h2>

    @if (TempData["SuccessMessage"] != null)
    {
        <div class="alert alert-success">
            @TempData["SuccessMessage"]
        </div>
    }

    <form action="@Url.Action("Login", "Account")" method="post">
        @Html.AntiForgeryToken()

        <div class="form-group">
            <label for="username">Username:</label>
            <input type="text" class="form-control" id="username" name="username" required />
        </div>

        <div class="form-group">
            <label for="password">Password:</label>
            <input type="password" class="form-control" id="password" name="password" required />
        </div>

        <button type="submit" class="btn btn-primary">Login</button>

        @if (!string.IsNullOrEmpty(ViewBag.Message))
        {
            <p class="text-danger">@ViewBag.Message</p>
        }
    </form>
</div>-->